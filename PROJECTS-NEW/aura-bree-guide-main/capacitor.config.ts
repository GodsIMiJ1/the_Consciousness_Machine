import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quantum.odyssey.aurabree',
  appName: 'Aura Bree Guide',
  webDir: 'dist'
};

export default config;
